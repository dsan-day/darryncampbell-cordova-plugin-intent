<?xml version='1.0' encoding='utf-8'?>
<plugin id="com-darryncampbell-cordova-plugin-intent" version="2.2.0" xmlns="http://apache.org/cordova/ns/plugins/1.0" xmlns:android="http://schemas.android.com/apk/res/android">
    <name>Intent Shim</name>
    <js-module name="IntentShim" src="www/IntentShim.js">
        <clobbers target="intentShim" />
    </js-module>
    
    <hook type="after_prepare" src="hooks/after_prepare.js" />
	
	<!-- android -->
    <platform name="android">


				<config-file target="res/xml/config.xml" parent="/*">
			<feature name="IntentShim">
				<param name="android-package" value="com.darryncampbell.cordova.plugin.intent.IntentShim"/>
				<param name="onload" value="true"/>
			</feature>
		</config-file>
		<!-- These modifications to AndroidManifest.xml are applied in the plugin.xml file of the Intent Plugin (Zebra). Background: - Cordova's "edit-config" command can cause conflicts when multiple plugins attempt to modify the same AndroidManifest.xml attribute. See: https://cordova.apache.org/docs/en/latest/plugin_ref/spec.html - When this conflict occurs, the affected manifest elements must be manually merged into a single plugin to avoid runtime issues. Decision: - We merged the conflicting elements into the Intent Plugin because it is referenced by all three mobile apps. - This prevents unnecessary references to the Camera Plugin in the Delivery App and the Sanday Authenticator, keeping dependencies cleaner. Summary: - All AndroidManifest.xml changes that overlap between plugins should be consolidated in this file (Intent Plugin) to avoid conflicts. -->
		<config-file target="AndroidManifest.xml" parent="/*">
			<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
			<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
			<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
		</config-file>


<!--
		
		<edit-config target="AndroidManifest.xml" parent="application">
			<provider android:name="org.apache.cordova.camera.FileProvider" android:authorities="${applicationId}.camera.provider" android:exported="false" android:grantUriPermissions="true">
				<meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/provider_paths"/>
			</provider>
			 <provider android:name="com.darryncampbell.cordova.plugin.intent.CordovaPluginIntentFileProvider" android:authorities="${applicationId}.darryncampbell.cordova.plugin.intent.fileprovider" android:exported="false" android:grantUriPermissions="true"> <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/provider_paths"/> </provider> 
		</edit-config>
-->

		<edit-config target="AndroidManifest.xml" parent="application">
    <!-- Camera provider -->
    <provider
        android:name="org.apache.cordova.camera.FileProvider"
        android:authorities="${applicationId}.camera.provider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/camera_provider_paths"/>
    </provider>

    <!-- Intent plugin provider -->
    <provider
        android:name="com.darryncampbell.cordova.plugin.intent.CordovaPluginIntentFileProvider"
        android:authorities="${applicationId}.darryncampbell.cordova.plugin.intent.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/provider_paths"/>
    </provider>
</edit-config>


		
		<edit-config target="AndroidManifest.xml" parent="/*">
			<queries>
				<intent>
					<action android:name="android.media.action.IMAGE_CAPTURE"/>
				</intent>
				<intent>
					<action android:name="android.intent.action.GET_CONTENT"/>
				</intent>
				<intent>
					<action android:name="android.intent.action.PICK"/>
				</intent>
			</queries>
		</edit-config>
		<edit-config target="AndroidManifest.xml" parent="/manifest/application/activity">
			<intent-filter>
				<action android:name="com.darryncampbell.cordova.plugin.intent.ACTION"/>
				<category android:name="android.intent.category.DEFAULT"/>
			</intent-filter>
		</edit-config>
		
		
        <source-file src="src/android/IntentShim.java" target-dir="src/com/darryncampbell/plugin/intent" />
        <source-file src="src/android/CordovaPluginIntentFileProvider.java" target-dir="src/com/darryncampbell/plugin/intent" />
		<resource-file src="src/android/res/xml/provider_paths.xml" target="res/xml/provider_paths.xml"/>
		<resource-file src="src/android/res/xml/camera_provider_paths.xml" target="res/xml/camera_provider_paths.xml"/>
		<framework src="androidx.core:core:1.1.0" />
    </platform>
	
</plugin>
